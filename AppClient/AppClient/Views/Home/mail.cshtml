@model IEnumerable<AppClient.Models.Mail>

@{
    ViewBag.Title = "صندوق پیام ها";
    Layout = "~/Views/Shared/_LayoutPage2.cshtml";
}

<legend></legend>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Resiver)
        </th>

       

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Resiver)
            </td>

       

            <td class="widthtd">
                @Html.ActionLink("مشاهده", "Show_mail", new { id = item.id }) |

                @Html.ActionLink("حذف", "delete_Mail", new { id = item.id })

            </td>
        </tr>
    }

</table>
